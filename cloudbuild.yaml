steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: build
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/bomed-world:latest'
      - '--build-arg'
      - 'NEXT_PUBLIC_WORLD_APP_ID=app_bcc263186d9a781025f30224632aa559'
      - '--build-arg'
      - 'NEXT_PUBLIC_WORLD_ACTION=verify-patient'
      - '--build-arg'
      - 'NEXT_PUBLIC_APP_URL=https://world.bomed.ai'
      - '--build-arg'
      - 'NEXT_PUBLIC_BOLO_API_URL=https://api.bolospot.com'
      - '.'

  # Push to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: push
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/bomed-world:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: deploy
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'bomed-world'
      - '--image'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/bomed-world:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--update-env-vars'
      - 'NEXT_PUBLIC_WORLD_APP_ID=app_bcc263186d9a781025f30224632aa559'
      - '--port'
      - '8080'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '3'

options:
  logging: CLOUD_LOGGING_ONLY
